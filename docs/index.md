# Horizon

A hobby x86_64 kernel written in C.

See the **API Reference** in the sidebar for types, functions, and files
extracted from the source tree (`kernel/`, `libc/`).

```{toctree}
:maxdepth: 2
:hidden:

api/library_root
```
